<div class="content-editor-toolbar">
	{{!-- Shortcode menu --}}
    	{{#paper-menu as |menu|}}

				{{#menu.trigger}}
				  {{#paper-button raised=true primary=true}}Shortcode einfügen{{/paper-button}}
				{{/menu.trigger}}

{{!-- Shortcodes --}}
				{{#menu.content width=5 as |content|}}
					<p class="centered">Verfügbare Shortcodes</p>
					{{#paper-list}}

					{{!-- Exkursion shortcodes --}}
							{{#if excursion}}
								{{#paper-item as |controls|}}
								  {{#controls.button secondary=true onClick=(action "insertStationstitel")}}
					          {{paper-icon "title"}}
					        {{/controls.button}}
								  <p>Exkursion Stationstitel</p>
								{{/paper-item}}
							{{/if}}

							{{!-- Themen shortcodes --}}
							{{#if topic}}
								{{#paper-item as |controls|}}
								  {{#controls.button secondary=true onClick=(action "insertTable")}}
					          {{paper-icon "border all"}}
					        {{/controls.button}}
								  <p>Tabelle einsetzen</p>
								{{/paper-item}}
							{{/if}}

							{{!-- Regionen shortcodes --}}
							{{#if region}}

							{{/if}}

							{{#paper-item as |controls|}}
				        <p>Absatz erzwingen</p>
				        {{#controls.button secondary=true onClick=(action "insertClear")}}
				          {{paper-icon "wrap text"}}
				        {{/controls.button}}
				      {{/paper-item}}

							{{paper-divider}}

							{{#paper-item as |controls|}}
				        <p>Querverlinkung einsetzen</p>
				        {{#controls.button secondary=true onClick=(action "insertRelref")}}
				          {{paper-icon "link"}}
				        {{/controls.button}}
				      {{/paper-item}}

							{{paper-divider}}

							{{#paper-item as |controls|}}
				        <p>Hyperlink einsetzen</p>
				        {{#controls.button secondary=true onClick=(action "insertHyperlink")}}
				          {{paper-icon "link"}}
				        {{/controls.button}}
				      {{/paper-item}}

							{{paper-divider}}


							{{#paper-item as |controls|}}
							  <p>Kommentar einsetzen</p>
							  {{#controls.button secondary=true onClick=(action "insertComment")}}
				          {{paper-icon "insert comment"}}
				        {{/controls.button}}
							{{/paper-item}}

							{{paper-divider}}

							{{#paper-item as |controls|}}
							  <p>Download einsetzen</p>
							  {{#controls.button secondary=true onClick=(action "insertDownload")}}
				          {{paper-icon "file download"}}
				        {{/controls.button}}
							{{/paper-item}}

							{{paper-divider}}

							{{#paper-item as |controls|}}
							  <p>Bildervergleich einsetzen</p>
							  {{#controls.button secondary=true onClick=(action "insertCompareImg")}}
				          {{paper-icon "flip"}}
				        {{/controls.button}}
							{{/paper-item}}

							{{paper-divider}}


					{{/paper-list}}
				{{/menu.content}}
			{{/paper-menu}}


  {{!-- Image assets menu --}}
    	{{#paper-menu as |menu|}}
			{{#menu.trigger}}
			  {{#paper-button raised=true primary=true}}Anhänge organisieren{{/paper-button}}
			{{/menu.trigger}}

			{{#menu.content width=5 as |content|}}
				<p class="centered">Neue Datei hinzufügen</p>
				{{#paper-list}}
				  {{#paper-item class="md-3-line"}}
				  	{{file-upload url=assetUrl updateAction="updateAction"}}
					{{/paper-item}}
					<br>

				{{#if model.images.length}}
			  {{#paper-subheader}}Anhänge für {{model.id}}{{/paper-subheader}}
			  {{else}}
			  {{#paper-subheader}}Keine Anhänge für {{model.id}}{{/paper-subheader}}
			  {{/if}}

        {{#each model.images as |image|}}
          {{#paper-item class="md-2-line" onClick=null as |controls|}}
            <img onclick={{action "openImage" image}} class="preview-img" src="{{image.src}}" alt="">
            <div class="md-list-item-text asset-viewer-list-item">
              <h4>{{image.filename}}</h4>
              <p class="text-underline" onclick={{action "deleteImage" image}}>Abbildung löschen</p>
            </div>
            {{#controls.button secondary=true iconButton=true onClick=(action "sendImageShortcode" image)}}
              {{paper-icon "image"}}
            {{/controls.button}}
          {{/paper-item}}
        {{/each}}


				{{/paper-list}}
			{{/menu.content}}

		{{/paper-menu}}

		{{!-- Undo/Redo actiond --}}
		{{!-- {{#paper-button raised=true mini=true onClick=(action "undo")}}
			{{paper-icon "undo"}}
		{{/paper-button}}

		{{#paper-button raised=true mini=true onClick=(action "redo")}}
			{{paper-icon "redo"}}
		{{/paper-button}} --}}
</div>
