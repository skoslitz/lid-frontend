
  {{#paper-menu as |menu|}}
    {{#menu.trigger}}
       <h4 class="meta-editor-button">alle anzeigen</h4>
    {{/menu.trigger}}

    {{#menu.content width=5 as |content|}}
      <h4 class="centered">Literatur {{model.id}}</h4>
      {{!-- <ul>
        {{#each model.literaturangaben as |literaturangabe|}}
        <li><p>{{literaturangabe}}</p></li>
        {{/each}}
      </ul> --}}

      {{#paper-list}}
        {{#each model.literaturangaben as |literaturangabe|}}
        {{#paper-item class="md-1-line"}}
            <div class="md-list-item-text">
              <p>{{literaturangabe}}</p>
            </div>
          {{/paper-item}}
        {{/each}}
      {{/paper-list}}

    {{/menu.content}}
  {{/paper-menu}}

