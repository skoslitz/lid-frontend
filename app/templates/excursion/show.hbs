{{#paper-card as |card|}}
  {{#card.title as |title|}}
    {{#title.text as |text|}}
      {{#text.headline}}Metadaten (Frontmatter){{/text.headline}}   
    {{/title.text}}
  {{/card.title}}
  {{#card.content}}
    <div layout="" layout-sm="column">
		{{paper-input flex=70 label="Titel" value=model.title onChange=""}}
		{{paper-input flex=30 label="Id" value=model.id onChange=""}}
	</div>
  {{/card.content}}
{{/paper-card}}

{{#paper-card as |card|}}
  {{#card.title as |title|}}
    {{#title.text as |text|}}
      {{#text.headline}}Content{{/text.headline}}   
    {{/title.text}}
  {{/card.title}}
  {{#card.content}}
   {{trix-editor class="trix-editor-wrapper"
              editorClass="trix-editor"
              value=model.content
              trix-change=(action "handleTrixAction") }}
  {{/card.content}}
{{/paper-card}}



